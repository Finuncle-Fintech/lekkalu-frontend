import { styled } from '@mui/system';
import { Button } from '@mui/material';

export const HeaderButton = styled(Button)({
  color: "white",
  "&:hover": { color: "white" },
  "&:active": { color: "white" },
});